import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function AdminOrdersPage() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Orders Management</CardTitle>
                <CardDescription>View and manage pending and active orders.</CardDescription>
            </CardHeader>
            <CardContent>
                <p>Order management interface will be built here.</p>
            </CardContent>
        </Card>
    )
}
